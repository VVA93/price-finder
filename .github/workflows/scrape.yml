name: scrape

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r ./requirements.txt   # 👈 явный путь

      - name: Ensure dirs
        run: |
          mkdir -p results
          mkdir -p logs

      - name: Run script
        run: python src/main.py

      - name: Upload results and logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results
          path: |
            results/**
            logs/**
